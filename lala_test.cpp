#include <iostream>
#include <cctype>
using namespace std;

int main()
{
    string questions [] = {
        {"If \(a\) and \(b\) are positive integers such that

$$ \gcd(a,b)=12,\qquad \operatorname{LCM}(a,b)=180 $$

which of the following cannot be the value of \(a+b\)?"},

    }
}